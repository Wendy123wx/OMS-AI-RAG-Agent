# 页面PRD：用户管理列表页

## 1. 页面信息

| 项目 | 内容 |
| --- | --- |
| 文件序号 | 05 |
| 页面编号 | P04-01 |
| 页面名称 | 用户管理列表页 |
| 所属模块 | 用户管理 |
| 所属功能模块 | 系统PRD 5.5.1「普通用户信息」、5.5.2「禁用与删除」、5.5.3「操作记录」（入口部分）、5.9.1「管理后台布局」 |
| 可访问角色 | 管理员 |
| 依据的领域模型上下文/聚合 | 身份与访问上下文：`UserAccount`（含 `Role`、`AccountStatus`、`Email`、`Avatar`、`Credential`）；领域服务 `AccountManagementService` |
| 布局容器 | 管理后台壳层 · 左侧菜单「用户管理」（见总览 §3.3） |
| 关联三级页面 | P04-03「12-前端-用户管理-新增编辑用户弹窗.md」（私有化弹窗）；P04-02「06-前端-用户管理-删除确认弹窗.md」（私有化弹窗） |

## 2. 页面定位与目标

用户管理列表页承载"管理员对普通用户账号进行新增、查看、修改、删除及禁用/恢复"的目标（PRD 5.5.1、5.5.2）。位于管理后台左侧菜单「用户管理」项下（PRD 5.9.1）。

**拆分依据**：列表 + 新增/编辑弹窗 + 删除确认弹窗。新增/编辑表单（C02）独立为 P04-03「12-前端-用户管理-新增编辑用户弹窗.md」；删除二级确认独立为 P04-02「06-前端-用户管理-删除确认弹窗.md」。

## 3. 使用角色与权限

- 仅管理员可访问（PRD 3、5.9.2）。
- 仅可作用于「普通用户」角色账号（PRD 5.5.1、9.5.19）。

## 4. 进入路径与导航关系

- **入口**：管理员在问答平台壳层点击顶栏「管理后台」Tab 进入管理后台壳层，再点击左侧菜单「用户管理」（见总览 Q3 默认项待确认）。
- **离开路径**：左侧菜单切换 P06/P07；右上角「← 返回工作台」→ P02；「历史操作」→ P05；删除 → 打开 P04-02。

## 5. 页面结构

本页为**管理后台壳层**右侧内容区在「用户管理」菜单项下的视图；壳层结构见总览 §3.3。

1. **管理后台壳层顶栏**：标题「管理后台」；右上角「← 返回工作台」。
2. **管理后台左侧菜单**（与 P06/P07 共享）：用户管理（当前激活）| 统计看板 | 查询记录。
3. **用户管理内容区**（布局简图）：
   - **列表与操作区**：普通用户列表（用户名、邮箱、头像、状态、创建时间、更新时间）；新增、编辑、禁用/恢复、删除操作。
   - **右上角「历史操作」入口**（PRD 5.5.3）→ P05 操作记录列表。
   - **新增/编辑入口**：点击后打开 P04-03 新增编辑用户弹窗（C02，见「12-前端-用户管理-新增编辑用户弹窗.md」）。
4. **禁用/恢复操作**：针对列表单条记录执行，完成后列表即时刷新（PRD 5.5.2）；无需二级确认弹窗（PRD 5.5.2 禁用可恢复）。

## 6. 关键组件清单

- 管理后台壳层框架（顶栏、左侧菜单、返回工作台）
- 普通用户列表、新增/编辑入口（触发 P04-03）
- 禁用/恢复操作、删除入口（触发 P04-02）
- 「历史操作」入口（跳转 P05）
- 列表即时刷新

## 7. 页面状态

- 正常态、空状态、P04-03 弹窗打开态、P04-02 弹窗打开态、操作后即时刷新态

## 8. 核心交互流程

1. 浏览普通用户列表。
2. 新增：打开 P04-03 弹窗，填写用户名/邮箱/头像/初始密码，提交创建。
3. 编辑：打开 P04-03 弹窗，修改邮箱/头像，提交保存。
4. 禁用/恢复：即时刷新列表并记入操作记录。
5. 删除：打开 P04-02 删除确认页，二级确认后执行删除。

## 9. 展示的业务数据

- `UserAccount`：`username`、`Email`、`Avatar`、`AccountStatus`、`createdAt`、`updatedAt`
- `Role`：约束仅操作普通用户
- `Credential`：新增时通过初始密码设置

## 10. 业务规则与约束

- 不含管理员账号管理（PRD 9.5.19）。
- 禁用可恢复；删除须 P04-02 二级确认（PRD 9.5.21、9.5.22）。
- `username` 系统内唯一（领域模型 3.1 不变量2）。

## 11. 异常与边界场景

- 列表为空、用户名重复拦截、删除未完成确认不执行。

## 12. 待确认事项

- 初始密码字段规则（Q1）、头像设置方式（Q6）、管理后台默认菜单（Q3）、头像下拉（Q4）、列表分页（Q7）。
