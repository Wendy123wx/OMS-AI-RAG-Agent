# 环境变量示例，复制为 .env.development / .env.production 后按需调整

# 是否启用前端 Mock（true 时通过 src/mocks 拦截请求，无需真实后端）
VITE_USE_MOCK=true

# 后端 API 基础路径
VITE_API_BASE_URL=/api

# 问答流式接口地址（供 composables/useSseStream.ts 使用）
VITE_QA_ASK_STREAM_URL=/api/qa/ask
