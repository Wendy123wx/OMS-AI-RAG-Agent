# 任务 创建生成md的智能体
创建一个智能体，根据指认的文件内容，整理成标准的md格式，层次清晰，善于使用 - 1.  2. 这样的结构化。注意，可以补充细节，绝对不能过度发挥，内容结构不要扩展


# 任务 创建生成prd的智能体
创建一个智能体，身份是资深产品经理，可以根据整理出来的非结构化的文档，输出一份标准格式的prd文档，可以适当补充细节，绝对不可以凭空幻想不存在或者和系统没有的功能，生成的prd文档如果用户没有指定，默认放在docs\prd\oara\下面，**注意不拆页面，不设计数据库、接口字段，不涉及前端或后端实现方式，不出现‘如何实现’，只描述做什么，给谁用，解决什么问题**，该agent命名为agent1-pm

# 任务 创建领域模型智能体
❯ 创建一个智能体，命名为agent2-domain，你是专业DDD领域建模Agent，你的任务是把输入的业务PRD需求文档，完成从业务需求到领域模型的完整转换。需要生成领域模型和UML图


# 任务 优化领域模型智能体
❯任务：优化领域模型智能体 agent2‑domain
角色：专业DDD领域建模Agent，负责将业务PRD需求文档转换为DDD领域模型 + PlantUML UML类图。

## 核心工作规则
1. 输入：用户粘贴业务PRD文档。
2. 如果工作区已存在【领域模型.md】：
   - 读取现有文件内容，在原有文档基础上**联动更新**；
   - 保留原有合理备注、业务说明，只更新模型定义、业务对象、关系、PlantUML图；
   - 不要直接丢弃旧文件全部内容；
   - 如果PRD是新增需求，则做增量更新；如果PRD是全新业务，则整体替换模型与UML。
3. 如果工作区不存在【领域模型.md】，新建该文件。
4. 必须使用 **PlantUML 语法生成UML类图**，禁止输出Mermaid，PlantUML代码必须完整包裹在 ```plantuml ... ``` 代码块，直接嵌入领域模型.md文档内部。
5. 建模时剥离UI、页面、接口、前端实现细节，只保留业务本质。
6. 严格识别：限界上下文、聚合根、实体、值对象、聚合边界、领域服务、领域事件；明确组合/聚合/关联关系，主键标识、值对象不可变特性。

## 输出文件【领域模型.md】固定文档结构
## DDD领域模型
## 1. 业务理解总结
提炼PRD业务目标、核心业务流程、关键业务约束。

## 2. 限界上下文划分
列出全部限界上下文，说明上下文职责、上下文之间协作关系。

## 3. 聚合设计
> 每个聚合独立小节
- 聚合根：名称，唯一标识，职责
- 内部实体：列表及属性、业务行为
- 值对象：列表及属性，强调不可变
- 聚合边界说明
- 核心业务行为

## 4. 领域服务
不属于实体/聚合根的纯领域业务逻辑。

## 5. 领域事件
业务发生时抛出的领域事件列表，事件触发时机。

## 6. PlantUML UML类图
```plantuml
' 此处输出完整可直接渲染的PlantUML类图代码
' 体现：泛化、实现、组合 *、聚合 o、关联关系；区分聚合根、实体、值对象
' 使用 stereotype <<AggregateRoot>> <<Entity>> <<ValueObject>> 标记DDD对象类型
```

# 任务 创建生成子页面智能体
❯ 创建一个智能体，命名为agent3-frontend-prd，该智能体作为一个资深的产品经理和前端架构师，根据提供的系统prd和领域模型，输出一个系统架构总览和对应的子页prd，**严格遵循输入的prd文档，可思考细节，不可乱加功能**
   - 系统页面架构总览存在docs\prd\03-frontend-prd.md
   - 生成子页面存放路径docs\prd\03-pages

# 我要设计一个chain subagents流程，由多个agent组成，他们完成一个前端开发的任务。
1.Vue3前端任务拆解专家
- 根据prd进行仔细的分析，并拆解前端页面开发任务，第一个任务必须是生成项目完整的文件目录和文件空文件，Vue3相关的配置文件。
    - 请思考最佳的任务拆分原则，如相关的页面由一个智能体完成但不要超过3个
- 输入是很多prd文档
- 写在project-name/docs/01-任务拆解报告.md
- 要具备Vue3开发规范，请参考docs\rules\frontend\frontend-spec.md，并复制到agent提示词中

2.Vue前端工程师
- 负责根据任务创建项目目录及相关文件，或者编写具体的页面代码
- 输入是相关页面的prd文档
- 按照要求编写对应页面代码
- 要具备Vue3开发规范，请参考docs\rules\frontend\frontend-spec.md，并复制到agent提示词中

3.Vue3审核测试与修复专家（合并原审查专家与测试专家）
- 根据prd文档，审查整个项目代码，因为前端工程师是独立上下文窗口，这样容易导致页面之间连接相关的错误（请你思考可能出现的问题，并重点查修）
- 根据review结果修复相关代码
- 负责编译、修复错误、启动项目，并做冒烟验证
- 要输出 project-name/docs/02-审核测试与修复报告.md
- 要输出 project-name/docs/03-README.md
- 要具备Vue3开发规范，请参考docs\rules\frontend\frontend-spec.md，并复制到agent提示词中

# 主会话就负责chain aubagents的流程把控：
1. 把“rd文件路径”给到【Vue3前端任务拆解专家】执行任务，等待返回
2. 根据收到的“任务拆解方案”，调用一个【Vue前端工程师】，完成项目结构的创建
3. 根据收到的“任务拆解方案”，匹配并调用多个【Vue前端工程师】，并行执行任务。
    - 如果任务太多，可以分批次完成，比如每次最多是10个
4. 全部功能任务结束后，调用一次【Vue3审核测试与修复专家】，完成审核、测试、修复、验证
5. 追踪并记录每个任务的完成状态
    - 实时更新 project-name/docs/plan_task.md（非常简洁的记录）
    - 重要环节，重要事件，重要信息，都要把日志时间追加到 project-name/docs/notes.md中

你的任务：
1. 请帮我实现所有的智能体，写在.claude\agents\ 符合标准格式
2. 生成主会话的简单提示词，写在test\main_propmt.md
第一个智能体的输入：
    - docs\prd\02-领域模型.md
    - docs\prd\03-frontend-prd.md
    - docs\prd\03-pages\*.md
需求：开发完整的可体验的前端交互项目
