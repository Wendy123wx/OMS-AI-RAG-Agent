1. 页面拆分规则：C01、C04 为独立页面，不可合并。其中 C01 对应当前用户个人历史记录页面，C04 对应管理员视角的全量用户查询记录页面，二者业务视角、数据权限与使用主体完全独立，需分开维护。
2. C02 支持业务合并，可整合至对应模块统一管理；C03 独立成单独页面，不与任何页面合并。
3. 目录结构规范：按业务编号建立独立子目录，实行结构化分层管理。
4. 命名规范统一整改：所有页面、资源统一使用中文语义命名；子页 PRD 扁平存放于 `docs/prd/03-pages/`，文件名格式 `NN-前端-模块-页面名称.md`（如 `01-前端-身份认证-登录页.md`），「前端」为固定字段，不创建业务子目录。
5. 同步对齐 Agent2 工程的整体命名规范，保证前后命名体系统一、风格一致、可维护性统一。